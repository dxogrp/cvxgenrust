__HEADER__

from pathlib import Path
import sys

import numpy as np
__SPARSE_IMPORT__

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT.parent) not in sys.path:
    sys.path.insert(0, str(ROOT.parent))

import __PACKAGE_NAME__ as cgr

# This best-effort import assumes the original CVXPY problem lives in `__PROBLEM_MODULE__.py`.
# Update it if you generated this crate from a differently named module.
from __PROBLEM_MODULE__ import __PROBLEM_IMPORTS__


def main() -> None:
    cgr.register_solve_method()
__PARAM_VALUE_SETUP__

    problem.solve(
        method="__SOLVER_METHOD_NAME__",
        updated_params=[__UPDATED_PARAMS__],
        warm_start=False,
    )
    print("status =", problem.status)
    print("value =", problem.value)
__VARIABLE_PRINTS__


if __name__ == "__main__":
    main()
